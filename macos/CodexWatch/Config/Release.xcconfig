#include "Base.xcconfig"
#include? "Local.xcconfig"

SWIFT_COMPILATION_MODE = wholemodule
CODE_SIGN_STYLE = Manual
CODE_SIGN_IDENTITY = Apple Distribution
PROVISIONING_PROFILE_SPECIFIER = CodexWatch App Store
